// Print getallenreeks die een bepaald patroon volgen tot een bepaald in geven geteal.
let maxGetal = window.prompt("Geef een getal)");
maxGetal = parseInt(maxGetal);
let getal = 1;
while (getal <= maxGetal){
    console.log (getal);
    getal = getal*2;
}
